view:
  template:
    <few-view id="callParentActionSub" src="./callParentActionSub"></few-view>
    <h3>Update from child:</h3>
    <code>${codeText}</code>

# model
model:
  codeText: update

# update
action:
  callParentActionSub:
    updateText:
      input:
        updatedText: ${scope.item.test_input}
      output:
        codeText: updatedText
