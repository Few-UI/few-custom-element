view:
  template:
    <h3>Update from parent:</h3>
    <code>${simpleText}</code>

# model
model:
  simpleText: Test String

action:
  updateText:
    input:
      value: ${scope.value}
    output:
      simpleText: value
