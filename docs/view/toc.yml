# view
view:
  template:
    <ul>
      <li f-each="item of items">
        <a onclick="${transientTo}">${item.name}</a>
      </li>
    </ul>

# action
action:
  transientTo:
    name: few.requestUpdate
    input:
      element: ${scope.element}
      data: ${scope.element.textContent}



