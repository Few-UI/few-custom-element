# view
view:
  template:
    <few-view src="view/frame" model="data"></few-view>

# model
model:
  data:
    primary: view/example
    secondary: view/example


# option
option:
# scopePath: scope
  actionPaths:
    - action
  stringTemplate:
    pattern: '/^\s*\${\s*([\S\s\r\n]*)\s*}\s*$/m'
    index: 1

