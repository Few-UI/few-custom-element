# view
viewHtml:
  <few-button click="testAction">{{data.testMsg}}</few-button>
  <few-bridge>
    <few-button click="testAction">{{data.testMsg}}</few-button>
  </few-bridge>

# view model
viewModel:
  data:
    testMsg: Hello World!

  function:
    testAction:
      import: sample/test
      name: testAccu
      input:
        message: '{{data.testMsg}}'
      output:
        data.testMsg: ''

